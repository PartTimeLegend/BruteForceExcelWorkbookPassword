name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag bruteforceexcelworkbookpassword:$(date +%s)
    # Linux Only maybe one day.
    # - name: Push to GitHub Packages
    #  uses: docker/build-push-action@v2
    #  with:
    #    username: ${{ github.actor }}
    #    password: ${{ secrets.GITHUB_TOKEN }}
    #    registry: docker.pkg.github.com
    #    repository: parttimelegend/choco-docker-test/choco-docker-test
    #    tag_with_ref: true